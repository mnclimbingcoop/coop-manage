<%@ page contentType="text/xml" %><hid:Cardholders action="AD" recordOffset="0" recordCount="2" moreRecords="false"><g:each var="c" in="${cardHolders}"><hid:Cardholder cardholderID="${c.cardholderID}" forename="${c.forename}" middleName="${c.middleName}" surname="${c.surname}" email="${c.email}" phone="${c.phone}" roleSetID="${c.roleSetID}" pinCommands="${c.pinCommands}" confirmingPinExempt="${c.confirmingPinExempt}" exemptFromPassback="${c.exemptFromPassback}" extendedAccess="${c.extendedAccess}"/></g:each></hid:Cardholders>